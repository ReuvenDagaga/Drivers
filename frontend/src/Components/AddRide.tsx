import { Box, Typography } from "@mui/material";

const AddRide = () => {
  return (
    <Box sx={{ mt: 3 }}>
      <Typography variant="h5">Request a Ride</Typography>
      <Typography variant="body1">Here you will be able to add a new ride request.</Typography>
    </Box>
  );
};

export default AddRide;
